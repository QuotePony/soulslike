<section class="sl-panel">
  <div class="sl-panel-title">Notes & Chronicle</div>
  <label class="sl-field full">
    <span class="sl-label">Notes</span>
    <textarea class="sl-input sl-textarea" name="system.note" rows="5" placeholder="Tactics, rumors, scars to remember.">{{system.note}}</textarea>
  </label>
  <label class="sl-field full">
    <span class="sl-label">Session Log</span>
    <textarea class="sl-input sl-textarea" name="system.log" rows="4" placeholder="Keep a trail for those who come after.">{{system.log}}</textarea>
  </label>
</section>
