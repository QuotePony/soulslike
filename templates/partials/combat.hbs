<div class="sl-gear-grid">
  <div class="hand-slot sl-card" data-hand="left">
    <div class="sl-card-head">
      <div class="sl-card-title">
        <h3>Left Hand</h3>
        <span class="sl-tooltip" title="Drag a weapon here. Two-handing (slotting the same weapon in both hands) boosts its damage; items tagged 2H need both hands.">?</span>
      </div>
      <button class="sl-icon-button" type="button" data-action="item-delete" title="Clear slot">
        X
      </button>
    </div>
    {{#if hands.left}}
      <div class="sl-hand-card" data-item-id="{{hands.left._id}}">
        <img src="{{hands.left.img}}" alt="{{hands.left.name}}" />
        <div class="sl-hand-meta">
          <div class="sl-hand-name">
            {{#if hands.left.name}}
              {{hands.left.name}}
            {{else}}
              {{hands.left}}
            {{/if}}
          </div>
          <div class="sl-hand-sub">
            {{#if hands.left.type}}
              {{hands.left.type}}
            {{else}}
              Drag a weapon here
            {{/if}}
          </div>
        </div>
      </div>
    {{else}}
      <div class="sl-slot-placeholder">
        Drop a weapon here
      </div>
    {{/if}}
  </div>

  <div class="hand-slot sl-card" data-hand="right">
    <div class="sl-card-head">
      <div class="sl-card-title">
        <h3>Right Hand</h3>
        <span class="sl-tooltip" title="Actions come from the weapon cards. Apply the item's damage type, stat, and stamina costs when you roll.">?</span>
      </div>
      <button class="sl-icon-button" type="button" data-action="item-delete" title="Clear slot">
        X
      </button>
    </div>
    {{#if hands.right}}
      <div class="sl-hand-card" data-item-id="{{hands.right._id}}">
        <img src="{{hands.right.img}}" alt="{{hands.right.name}}" />
        <div class="sl-hand-meta">
          <div class="sl-hand-name">
            {{#if hands.right.name}}
              {{hands.right.name}}
            {{else}}
              {{hands.right}}
            {{/if}}
          </div>
          <div class="sl-hand-sub">
            {{#if hands.right.type}}
              {{hands.right.type}}
            {{else}}
              Drag a weapon here
            {{/if}}
          </div>
        </div>
      </div>
    {{else}}
      <div class="sl-slot-placeholder">
        Drop a weapon here
      </div>
    {{/if}}
  </div>
</div>
<p class="sl-help">Ammunition-tagged actions spend ammo; refill an ammo type for 1 supply. Reactions marked with R resolve immediately when their trigger happens.</p>
