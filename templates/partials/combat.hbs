<div class="sl-gear-grid">
  <div class="hand-slot sl-card" data-hand="left">
    <div class="sl-card-head">
      <h3>Left Hand</h3>
      <button class="sl-icon-button" type="button" data-action="item-delete" title="Clear slot">
        �o
      </button>
    </div>
    {{#if hands.left}}
      <div class="sl-hand-card" data-item-id="{{hands.left._id}}">
        <img src="{{hands.left.img}}" alt="{{hands.left.name}}" />
        <div class="sl-hand-meta">
          <div class="sl-hand-name">
            {{#if hands.left.name}}
              {{hands.left.name}}
            {{else}}
              {{hands.left}}
            {{/if}}
          </div>
          <div class="sl-hand-sub">
            {{#if hands.left.type}}
              {{hands.left.type}}
            {{else}}
              Drag a weapon here
            {{/if}}
          </div>
        </div>
      </div>
    {{else}}
      <div class="sl-slot-placeholder">
        Drop a weapon here
      </div>
    {{/if}}
  </div>

  <div class="hand-slot sl-card" data-hand="right">
    <div class="sl-card-head">
      <h3>Right Hand</h3>
      <button class="sl-icon-button" type="button" data-action="item-delete" title="Clear slot">
        �o
      </button>
    </div>
    {{#if hands.right}}
      <div class="sl-hand-card" data-item-id="{{hands.right._id}}">
        <img src="{{hands.right.img}}" alt="{{hands.right.name}}" />
        <div class="sl-hand-meta">
          <div class="sl-hand-name">
            {{#if hands.right.name}}
              {{hands.right.name}}
            {{else}}
              {{hands.right}}
            {{/if}}
          </div>
          <div class="sl-hand-sub">
            {{#if hands.right.type}}
              {{hands.right.type}}
            {{else}}
              Drag a weapon here
            {{/if}}
          </div>
        </div>
      </div>
    {{else}}
      <div class="sl-slot-placeholder">
        Drop a weapon here
      </div>
    {{/if}}
  </div>
</div>
